import React from "react";
import {Grid, Typography} from "@material-ui/core";

function Target() {

    return (
        <Grid item>
            <Typography>Пост</Typography>
        </Grid>
    );
}

export default Target;